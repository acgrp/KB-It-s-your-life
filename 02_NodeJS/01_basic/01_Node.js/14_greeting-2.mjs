const hi = (name) => {
  console.log(`${name}님, 안녕하세요?`);
};

const goodbye = (name) => {
  console.log(`${name}님, 안녕히 가세요.`);
};

// 보내기
// export const hii = hi;
// export const goodbye2 = goodbye;

export default { hi, goodbye };
